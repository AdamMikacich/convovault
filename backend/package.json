{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "Store conversations in a vault",
  "main": "app.js",
  "scripts": {
    "start": "DEBUG=error,app,http,db,bot nodemon app.js --ignore 'data.json'",
    "winstart": "set DEBUG=error,app,http,db,bot && nodemon app.js --ignore 'data.json'"
  },
  "author": "Adam Mikacich",
  "dependencies": {
    "debug": "^4.1.1",
    "express": "^4.17.1",
    "mysql2": "^2.1.0",
    "sequelize": "^6.3.3",
    "slack": "^11.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
